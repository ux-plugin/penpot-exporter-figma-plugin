/**
 * Public lib entry: type definitions only (PenpotNode, Matrix, etc.).
 * Used by skia-rs-wasm and figma-adapter for types.
 * For plugin runtime (transformSceneNode, etc.) use 'penpot-exporter/plugin'.
 */
export * from '../ui-src/types/index';
